from django.urls import path

app_name = 'public'

urlpatterns = [
    # path('', registration, name='user-registration-api'),
    # path('/login', login, name='user-login-api'),
    # path('/refresh_token', refreshed_token, name='user-token-refresh-api'),
    # path('/verify', verify, name='user-verify-api'),
    # path('/pass-reset-otp', reset_password_otp_send, name='user-pass-reset-otp-send-api'),
    # path('/pass-reset', reset_password_verify_otp, name='user-pass-reset-api'),


]


